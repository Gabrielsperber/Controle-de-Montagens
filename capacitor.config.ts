import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gabrielsperber.controledemontagens',
  appName: 'Controle de Montagens',
  webDir: 'www',
  backgroundColor: '#1c2128',
  android: {
    backgroundColor: '#1c2128',
  },
};

export default config;
